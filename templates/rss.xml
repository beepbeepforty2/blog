<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0">
  <channel>
    <title>{{ config.title }}</title>
    <link>{{ config.base_url | escape_xml | safe }}/</link>
    <description>{{ config.description }}</description>
    {% for page in pages %}
      <item>
        <title>{{ page.title | escape_xml | safe }}</title>
        <link>{{ page.permalink | escape_xml | safe }}</link>
        <guid isPermaLink="true">{{ page.permalink | escape_xml | safe }}</guid>
        <description>{{ page.description | escape_xml | safe }}</description>
        <pubDate>{{ page.date | date(format="%a, %d %b %Y %H:%M:%S GMT", timezone="UTC") }}</pubDate>
      </item>
    {% endfor %}
  </channel>
</rss>
