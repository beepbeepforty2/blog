{
  "name": "blog",
  "type": "module",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "bash scripts/zola.sh serve --drafts",
    "build": "bash scripts/build.sh",
    "preview": "python3 -m http.server 4321 --directory dist",
    "check": "bash scripts/zola.sh check --skip-external-links && node scripts/validate-content.mjs"
  },
  "dependencies": {
    "katex": "0.18.4"
  }
}
